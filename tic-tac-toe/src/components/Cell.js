const Cell =({ id, cell}) => {
   
    const handleClick = () => {
        
    }

    return (
        <div className="square" id={id} onClick={handleClick}>

        </div>
    )
}

export default Cell